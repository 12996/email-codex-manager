# 2026-07-23 协议注册单并发队列

- 协议注册改为内存 FIFO 单并发队列；服务停止即丢弃队列。
- `POST /replacement-accounts/:id/register-protocol` 改为返回 `202` 入队结果；重复账号返回冲突。
- 新增队列查询和清空等待任务接口；补号管理页显示当前、等待、最近结果及行内“注册中/排队中”。
- 修复协议注册日志未展示的问题：服务端将协议注册 `onLog` 回调写入任务快照，前端轮询后将当前执行账号的 stdout、stderr 和准备步骤显示在“当前协议注册日志”面板；队列面板只保留任务状态和顺序。单任务最多保留 200 条内存日志。
- 验证：队列、API、前端专项 Node 测试 55/55 通过，Node 语法检查通过。
