# 2026-06-01-补号账号后端

- 状态：done
- 目标：实现补号账号后端数据库层、服务适配层、JSON API、测试和接口文档。
- 修改文件：`src/db.js`、`src/replacementAccounts.js`、`src/replacementServices.js`、`src/server.js`、`test/replacementAccounts.test.js`、`test/replacementServices.test.js`、`test/replacementAccountsApi.test.js`、`docs/project/api.md`、`docs/changes/`、`docs/work/`
- 验证结果：新增并通过补号账号 Repository、服务适配器、API 测试；全量测试通过。
- 未完成 / 风险：真实自动化补号接口尚未完成，当前 `/replacement-accounts/:id/replace` 通过 `replacementServices.replaceAccount` 保留适配边界，未配置时返回 `REPLACE_NOT_CONFIGURED`。
- 下一步：前端可按 `docs/project/api.md` 接入补号账号页面；真实自动化补号 JS 文件完成后接入 `replacementServices`。
- 日终交接：完成后更新 `handoff.md`
