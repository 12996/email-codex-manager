# 2026-06-01-补号账号前端页面

- 状态：done
- 目标：按用户截图实现补号账号前端页面，前端文件放在 `web/` 目录，并接入已实现的补号账号后端 API。
- 修改文件：`web/index.html`、`web/styles.css`、`web/app.js`、`src/server.js`、`test/replacementAccountsWeb.test.js`、`docs/project/api.md`、`docs/changes/`、`docs/work/`
- 验证结果：`test/replacementAccountsWeb.test.js` 通过；全量 `npm test` 通过。
- 未完成 / 风险：真实自动化补号适配器仍待接入；当前前端调用 `/replacement-accounts/:id/replace`，由后端适配层决定真实执行结果。
- 下一步：如需要更接近截图，可继续补充分页、列配置持久化、批量选择策略和真实操作日志。
- 日终交接：完成后更新 `handoff.md`
