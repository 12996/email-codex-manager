# handoff.md

状态：active

- 来源工作日志：`docs/work/2026-06-02-roxy-openai登录页邮箱处理与超时判断.md`
- 当前任务：Roxy OAuth 登录流程已串接到 OAuth callback、token exchange 和本地 CPA JSON 保存；验证码 API 已补充公开 key 与本机免登录调用能力；补号接口已通过子进程接入 Roxy OAuth 自动化；补号子进程日志页面和停止按钮已实现；`CHG-007` 至 `CHG-016` 已合并进 `PRD-002` 基线，`CHG-017` 已实现待后续 PRD 合并。
- 当前进展：已实现 `processOAuthLoginFlow` 状态机，覆盖 OpenAI 邮箱输入、邮箱验证码、手机验证方式选择、手机验证码、Codex 授权确认、OAuth callback 捕获、`exchangeToken` 和认证 JSON 保存；实机运行 `node .\src\auto\roxy_oauth_login.js` 已成功生成本地 CPA/sub2api JSON。验证码侧新增 `GET /api/verification-code/public/latest?key=...`，通过补号账号表放权 key 获取验证码；`POST /api/verification-code/latest` 本机请求免 `admin_auth`。`POST /replacement-accounts/:id/replace` 默认通过 `replacementServices` 子进程适配器执行 `src/auto/roxy_oauth_login.js`，从补号账号行传入 `ROXY_OAUTH_EMAIL` 和 `PHONE_VERIFICATION_SMS_API_URL`。新增 `replacement_automation_runs` 运行表、`data/automation-logs/` 子进程日志文件、`/replacement-automation-logs` 页面和 `/replacement-automation-runs*` API，可查看 stdout/stderr 并停止当前服务会话内仍活跃的 child。PRD 基线已吸收 Roxy 自动化、公开验证码、失败截图、认证 JSON 和子进程补号等长期需求。
- 关键文件：`src/auto/roxy_oauth_login.js`、`src/replacementServices.js`、`src/replacementAutomationRuns.js`、`test/replacementServices.test.js`、`test/roxyOauthLogin.test.js`、`src/server.js`、`src/replacementAccounts.js`、`src/db.js`、`web/automation-logs.html`、`web/automation-logs.js`、`test/verificationCodeApi.test.js`、`test/replacementAccounts.test.js`、`docs/changes/CHG-014-roxy-oauth-cpa-json-flow.md`、`docs/changes/CHG-015-public-verification-code-key.md`、`docs/changes/CHG-016-replacement-child-process-automation.md`、`docs/changes/CHG-017-replacement-automation-log-page.md`、`docs/work/2026-06-02-roxy-openai登录页邮箱处理与超时判断.md`
- 关键产物：`src/auto/product_files/cpa/jregkolpig+s4@gmail.com.json`、`src/auto/product_files/sub2api/jregkolpig+s4@gmail.com.json`。这些文件包含敏感 token，禁止提交或公开。
- 下一步建议：使用真实补号账号点击 `/replacement-accounts/:id/replace` 做端到端验证，并打开 `/replacement-automation-logs` 确认日志实时写入和停止按钮行为；提供 `MANAGEMENT_KEY` 后接入 CPA 后端 `POST /v0/management/auth-files?name=<account>.json` 上传。
