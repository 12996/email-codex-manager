# 2026-06-01-邮箱账号页面合并到web

- 状态：done
- 目标：将原 `src/views.js` 中的邮箱账号管理页面迁移到 `web/` 目录，并适配当前补号管理页面的后台风格。
- 修改文件：`web/accounts.html`、`web/accounts.js`、`src/server.js`、`test/accountsWebApi.test.js`、`docs/project/api.md`、`docs/changes/`、`docs/work/`
- 验证结果：`test/accountsWebApi.test.js` 通过；全量 `npm test` 通过。已补充验证：新版页面保留邮件读取位置和数量控件，并在测试连接/获取邮件时提供即时反馈；邮箱账号表格保留 Gmail 密码、2FA、App Password 三列显示。邮件列表默认数量调整为 5，邮箱账号表格和邮件结果区域固定 5 条可视内容后内部滚动，邮件结果默认只显示摘要行，点击后展开完整内容。
- 未完成 / 风险：`src/views.js` 仍保留登录页和旧表单兼容逻辑；如后续完全前端化登录页，可再拆除旧账号页面渲染函数。
- 下一步：未合并的 `implemented` change 已达到 5 个，应执行 PRD 基线合并。
- 日终交接：完成后更新 `handoff.md`
